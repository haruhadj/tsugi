# dependencies
node_modules/
.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts
.vercel

# env — everything except the example
.env
.env.*
!.env.example

# build / test output
/build
/coverage
*.tsbuildinfo

# agent tooling — local only. Also in the user's global ignore, but that
# does not travel with a clone, so state it here too.
.claude/settings.local.json
.serena/

# os / editor
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.env*
